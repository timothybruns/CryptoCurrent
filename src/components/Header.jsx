import React from 'react';
import '../App.js';

const Header  = props => {
	return (
      <div className="header">
       <h1 className="blogWelcome"> CryptoCurrent</h1>
       </div>
		);
};

export default Header;
